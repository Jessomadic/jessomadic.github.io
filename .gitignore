# Firebase config is injected at deploy time via GitHub Actions secrets.
# Never commit real Firebase credentials to this repo.
plex/firebase-config.js

# Local tool configuration can contain private network details or API keys.
tools/config.json
